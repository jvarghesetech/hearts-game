[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "hearts-game"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = ["rich"]

[project.scripts]
playheart = "main:main"
